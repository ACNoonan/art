function setup() {
    createCanvas(windowWidth, windowHeight);
    colorMode(HSB);
    background(0,0,162);
    frameRate(5);
    //noLoop();
    }
    
    
    // Euclid's 1.3 Proposition: Given two unequal straight lines, cut off from the greater straight line (AB) equal in length to the lesser (C);
    
    function draw() {
    
    
    }